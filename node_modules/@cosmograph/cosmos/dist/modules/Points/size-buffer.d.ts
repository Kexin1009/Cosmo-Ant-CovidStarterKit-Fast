import regl from 'regl';
import { NumericAccessor } from "../../config";
import { InputNode, Node } from "../../types";
export declare function createSizeBuffer<N extends InputNode>(nodes: Node<N>[], reglInstance: regl.Regl, pointTextureSize: number, sizeAccessor: NumericAccessor<Node<N>>): regl.Framebuffer2D;
