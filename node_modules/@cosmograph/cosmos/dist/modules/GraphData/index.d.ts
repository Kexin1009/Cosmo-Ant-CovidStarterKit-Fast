import { Node, Link, InputNode, InputLink } from "../../types";
export declare class GraphData<N extends InputNode, L extends InputLink> {
    private _nodes;
    private _links;
    setData(inputNodes: InputNode[], inputLinks: InputLink[]): void;
    get nodes(): Node<N>[];
    get links(): Link<N, L>[];
    findNodeById(id: string): Node<N> | undefined;
}
