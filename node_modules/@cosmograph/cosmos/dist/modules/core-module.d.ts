import regl from 'regl';
import { GraphConfigInterface } from "../config";
import { GraphData } from "./GraphData";
import { Points } from "./Points";
import { Store } from "./Store";
import { Node, Link, InputNode, InputLink } from "../types";
export declare class CoreModule<N extends InputNode, L extends InputLink> {
    readonly reglInstance: regl.Regl;
    readonly config: GraphConfigInterface<Node<N>, Link<N, L>>;
    readonly store: Store;
    readonly data: GraphData<N, L>;
    readonly points: Points<N, L> | undefined;
    constructor(reglInstance: regl.Regl, config: GraphConfigInterface<Node<N>, Link<N, L>>, store: Store, data: GraphData<N, L>, points?: Points<N, L>);
}
